@model SchoolManagement.mvc.DataDB.TeacherAllocationTable

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>TeacherAllocationTable</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="TaId" />
            <div class="form-group">
                <label asp-for="CoId" class="control-label"></label>
                <select asp-for="CoId" class="form-control" asp-items="ViewBag.CoId"></select>
                <span asp-validation-for="CoId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="TId" class="control-label"></label>
                <select asp-for="TId" class="form-control" asp-items="ViewBag.TId"></select>
                <span asp-validation-for="TId" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
